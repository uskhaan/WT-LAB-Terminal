extends ../layout

block content
  h1 Login
  p(style="color:red")= error
  form(action="/login",method="post")        
        .form-group
            label(for="email") Email
            input#email(type="text", class="form-control", name="email" value="umar@gmail.com")
        .form-group
            label(for="password") Password
            input#price(type="password", class="form-control", name="password", value="umars")
        input(type="submit", class="btn btn-info" value="Login")
